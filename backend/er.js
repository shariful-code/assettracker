// Department (1) ───< Employee (Many)
//    │
//    │
//    └──────────< AssetAssingmentDepartment (Many)
//                     │
//                     │
//                   Asset (Many) ───< AssetAssingmentEmployee (Many) >── Employee (Many)

// Category (1) ───< SubCategory (Many)
//    │
//    │
//    └──────────< Asset (Many)

// Brand (1) ───< Asset (Many)

// Vendor (1) ───< Asset (Many)





// Departments → create / read / update / delete  done

// Categories → create / read / update / delete done

// SubCategories → create / read / update / delete  done

// Brands → create / read / update / delete  done

// Vendors → create / read / update / delete done

// Employees → create / read / update / delete done

// Assets → create / read / update / delete  done

// AssetAssingmentEmployee → assign / unassign / list (CRUD is optional, mostly assign/unassign)

// AssetAssingmentDepartment → assign / unassign / list